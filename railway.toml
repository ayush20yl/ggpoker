[build]
builder = "NIXPACKS"
buildCommand = "cd server && npm install && npx --yes typescript@latest tsc"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[environments.production.variables]
NODE_ENV = "production"